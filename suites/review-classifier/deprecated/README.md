# `deprecated/` landing zone

Retired review-classifier suites land here (`suites/review-classifier/deprecated/<original-suite>/`, ids never renumbered). This directory carries no `suite.json`, so discovery ignores it; the procedure is in [docs/hygiene-gates.md](../../../docs/hygiene-gates.md).
