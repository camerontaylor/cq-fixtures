CI artifacts: per-role comparison tables (JSON + rendered markdown) from suite runs (plan §3.2; ws-j item 6).
Everything here is gitignored except this contract — the suite.yml `snapshot` job (dispatch/schedule only) commits tables to `reports/snapshots/<utc-date>/<role>/<suite>/<role>.table.json` now; J4 (phase-4-lane-j) remains the full-matrix goal.
