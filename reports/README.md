CI artifacts: per-role comparison tables (JSON + rendered markdown) from suite runs (plan §3.2; ws-j item 6).
Everything here is gitignored except this contract — the suite.yml `snapshot` job commits dispatch-run tables to `reports/snapshots/<utc-date>/<model>/<driver>/<role>/<suite>/<role>.table.json`. The real matrix and snapshot publisher are dispatch-only while the weekly matrix is paused per D10; the planned schedule re-enable is W6.4/W6.7.

J5 (phase-4 lane J) is the full five-cell axes matrix (ADR-0001 as revised 2026-09-14 — axis 1: models glm-5.3-flash / deepseek-flash on the ai-sdk driver; axis 2: drivers ai-sdk / claude-agent / subprocess / acp on the fixed served GLM id glm-5.3-flash, Z.AI only). The ai-sdk@glm-5.3-flash cell carries both axes' evidence and runs once. `workflow_dispatch` triggers a run; the weekly schedule does not while D10's pause remains in force. Snapshot data lands on the `snapshots` branch (main is hook-protected). The snapshot job copies each suite's comparison tables, `rows.jsonl`, `run.json`, journal, patches, and outputs so committed replay has all of its evidence and provenance inputs. Rendering the per-role tables into a README beside the JSON is a delivery-time step of each snapshot PR (the lane's review flow) — not workflow machinery.
