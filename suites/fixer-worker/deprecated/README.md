# `deprecated/` landing zone

Retired fixer-worker suites land here (`suites/fixer-worker/deprecated/<original-suite>/`, ids never renumbered). This directory carries no `suite.json`, so discovery ignores it; the procedure is in [docs/hygiene-gates.md](../../../docs/hygiene-gates.md).
