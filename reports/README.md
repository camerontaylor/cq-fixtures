CI artifacts: per-role comparison tables (JSON + rendered markdown) from suite runs (plan §3.2; ws-j item 6).
Everything here is gitignored except this contract — the suite.yml `snapshot` job (dispatch/schedule only) commits tables to `reports/snapshots/<utc-date>/<model>/<driver>/<role>/<suite>/<role>.table.json`.

J5 (phase-4 lane J) is the live full matrix: the five-cell axes matrix (ADR-0001 as revised 2026-09-14 — axis 1: models glm-5.3-flash / deepseek-chat on the ai-sdk driver; axis 2: drivers ai-sdk / claude-agent / subprocess / acp on the fixed served GLM id glm-5.3-flash, Z.AI only). The ai-sdk@glm-5.3-flash cell carries both axes' evidence and runs once. `workflow_dispatch` or the weekly schedule triggers a run; tables land on the `snapshots` branch (main is hook-protected), and each snapshot PR renders the per-role tables in a README next to the JSON.
