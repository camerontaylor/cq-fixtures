# `quarantine/` landing zone

Fixer-worker suites pulled for flakiness land here (`suites/fixer-worker/quarantine/<suite>/` with a dated `QUARANTINE.md`). This directory carries no `suite.json`, so discovery ignores it; the protocol is in [docs/hygiene-gates.md](../../../docs/hygiene-gates.md).
