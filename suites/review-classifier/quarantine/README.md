# `quarantine/` landing zone

Review-classifier suites pulled for flakiness land here (`suites/review-classifier/quarantine/<suite>/` with a dated `QUARANTINE.md`). This directory carries no `suite.json`, so discovery ignores it; the protocol is in [docs/hygiene-gates.md](../../../docs/hygiene-gates.md).
