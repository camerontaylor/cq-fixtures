{
  "bug_type": "missing logic",
  "failure_symptoms": "merge copies dangerous keys from the source, so a crafted payload can pollute Object.prototype.",
  "operator": "remove-conditional",
  "difficulty": "medium",
  "provenance": {
    "origin": "public-bug-canary",
    "generator": "canary:public-bug-class",
    "seed": 0,
    "engine_version": "synthetic-canary",
    "reference": "lodash prototype-pollution advisories CVE-2019-10744 / CVE-2018-3721 (behavior only)",
    "license": "no code vendored; reference is documentation only"
  },
  "validation": {
    "f2p": [
      "does not pollute Object.prototype"
    ],
    "p2p": [
      "merges nested plain objects"
    ],
    "fix": {
      "src/merge.ts": "// Recursive object merge — a synthetic reproduction of the classic\n// prototype-pollution bug class (lodash CVE-2019-10744 / CVE-2018-3721\n// behavior, no lodash code vendored). The clean version refuses to walk the\n// dangerous keys so a crafted source cannot reach Object.prototype.\nexport function merge(target: Record<string, unknown>, source: Record<string, unknown>): Record<string, unknown> {\n  for (const [key, value] of Object.entries(source)) {\n    if (key === '__proto__' || key === 'constructor' || key === 'prototype') continue;\n    if (isPlainObject(value) && isPlainObject(target[key])) {\n      merge(target[key] as Record<string, unknown>, value);\n    } else if (isPlainObject(value)) {\n      target[key] = merge({}, value);\n    } else {\n      target[key] = value;\n    }\n  }\n  return target;\n}\n\nfunction isPlainObject(value: unknown): value is Record<string, unknown> {\n  return typeof value === 'object' && value !== null && !Array.isArray(value);\n}\n"
    }
  },
  "adequacy": {
    "file": "src/merge.ts",
    "delete": "if (key === '__proto__' || key === 'constructor' || key === 'prototype') continue;"
  },
  "tell_audit": {
    "critic": "lane-leader adversarial diff read (tests withheld)",
    "verdict": "not-told",
    "notes": "Canary: the prototype-pollution bug class, reproduced synthetically."
  }
}
