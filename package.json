{
  "name": "cq-fixtures",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Scored per-role task suites, fixtures, schemas and eval runner for cq-toolkit.",
  "license": "MIT",
  "engines": {
    "node": ">=23.6"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "vitest run"
  },
  "dependencies": {
    "@camerontaylor/cq-toolkit": "file:vendor/cq-toolkit-0.0.0.tgz",
    "zod": "4.6.4"
  },
  "devDependencies": {
    "@anthropic-ai/claude-agent-sdk": "0.3.270",
    "@types/node": "^26.5.1",
    "ajv": "^8.20.0",
    "ajv-formats": "^3.0.1",
    "eslint": "^10.10.0",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.70.0",
    "vitest": "^5.0.0"
  }
}
