Result-row + per-role comparison-table JSON schemas (plan §3.2; ws-j item 2, field list in plan §8).
Empty scaffold — defined by J1 (phase-2-lane-j); the repo README's "schema: see schema/ (phase 2)" pointer resolves here then.
