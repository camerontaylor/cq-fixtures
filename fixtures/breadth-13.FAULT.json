{
  "bug_type": "operator misuse",
  "failure_symptoms": "averageWordLength returns a value far larger than the average word length.",
  "operator": "arithmetic-swap",
  "difficulty": "medium",
  "provenance": {
    "origin": "operator-catalog",
    "generator": "catalog:babel:ArithmeticOperator",
    "seed": 113,
    "engine_version": "@stryker-mutator/instrumenter@10.0.0"
  },
  "validation": {
    "f2p": [
      "averages the word lengths"
    ],
    "p2p": [
      "counts words ignoring surrounding whitespace",
      "lowercases and hyphen-joins words",
      "returns the text unchanged at the limit"
    ],
    "fix": {
      "src/aggregate.ts": "export function averageWordLength(text: string): number {\n  const words = text.trim().split(/\\s+/);\n  const total = words.reduce((sum, word) => sum + word.length, 0);\n  return total / words.length;\n}\n"
    }
  },
  "adequacy": {
    "file": "src/aggregate.ts",
    "delete": "return total / words.length;"
  },
  "tell_audit": {
    "critic": "lane-leader adversarial diff read (tests withheld)",
    "verdict": "not-told",
    "notes": "A division-to-multiplication swap; the average contract is test-only."
  }
}
