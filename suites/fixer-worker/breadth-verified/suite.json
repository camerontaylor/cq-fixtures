{
  "name": "breadth-verified",
  "role": "fixer-worker",
  "provenance": {
    "origin": "catalog-built synthetic (F2 seed + F3, 2026-09-21): 6 easy / 4 medium / 2 hard; every fault independently reproduced from FAULT.json alone (see REPRODUCTION.md); zero-dependency multi-module TS packages; no public-benchmark content"
  },
  "cases": [
    {
      "id": "breadth-01",
      "fixture": "fixtures/breadth-01",
      "task": {
        "prompt": "The workspace at the path below contains a TypeScript package whose vitest suite fails. Fix the source so the vitest suite passes (the scoring harness runs the suite for you; you do not need to install anything). Do not modify the tests. rectangleArea returns the wrong value for non-square rectangles; it must return width times height.",
        "notes": "easy · arithmetic-swap · operator misuse · canonical fix in fixtures/breadth-01.FAULT.json"
      },
      "probe": {
        "kind": "check-rerun",
        "check": "fixtures/breadth-01/check.mjs"
      }
    },
    {
      "id": "breadth-02",
      "fixture": "fixtures/breadth-02",
      "task": {
        "prompt": "The workspace at the path below contains a TypeScript package whose vitest suite fails. Fix the source so the vitest suite passes (the scoring harness runs the suite for you; you do not need to install anything). Do not modify the tests. isLastPage never reports the final page index as the last page; the last page index must be treated as final.",
        "notes": "easy · equality-boundary · operator misuse · canonical fix in fixtures/breadth-02.FAULT.json"
      },
      "probe": {
        "kind": "check-rerun",
        "check": "fixtures/breadth-02/check.mjs"
      }
    },
    {
      "id": "breadth-03",
      "fixture": "fixtures/breadth-03",
      "task": {
        "prompt": "The workspace at the path below contains a TypeScript package whose vitest suite fails. Fix the source so the vitest suite passes (the scoring harness runs the suite for you; you do not need to install anything). Do not modify the tests. discountRate gives members the full rate and non-members the discount; members must get ten percent off and non-members nothing.",
        "notes": "easy · ternary-swap · operator misuse · canonical fix in fixtures/breadth-03.FAULT.json"
      },
      "probe": {
        "kind": "check-rerun",
        "check": "fixtures/breadth-03/check.mjs"
      }
    },
    {
      "id": "breadth-04",
      "fixture": "fixtures/breadth-04",
      "task": {
        "prompt": "The workspace at the path below contains a TypeScript package whose vitest suite fails. Fix the source so the vitest suite passes (the scoring harness runs the suite for you; you do not need to install anything). Do not modify the tests. backoffDelay grows too fast; each attempt must double the previous delay.",
        "notes": "easy · constant-delta (trivial-prone, easy-only) · value misuse · canonical fix in fixtures/breadth-04.FAULT.json"
      },
      "probe": {
        "kind": "check-rerun",
        "check": "fixtures/breadth-04/check.mjs"
      }
    },
    {
      "id": "breadth-05",
      "fixture": "fixtures/breadth-05",
      "task": {
        "prompt": "The workspace at the path below contains a TypeScript package whose vitest suite fails. Fix the source so the vitest suite passes (the scoring harness runs the suite for you; you do not need to install anything). Do not modify the tests. allInStock reports true whenever any item is in stock; it must be true only when every item is in stock.",
        "notes": "medium · method-swap · function misuse · canonical fix in fixtures/breadth-05.FAULT.json"
      },
      "probe": {
        "kind": "check-rerun",
        "check": "fixtures/breadth-05/check.mjs"
      }
    },
    {
      "id": "breadth-06",
      "fixture": "fixtures/breadth-06",
      "task": {
        "prompt": "The workspace at the path below contains a TypeScript package whose vitest suite fails. Fix the source so the vitest suite passes (the scoring harness runs the suite for you; you do not need to install anything). Do not modify the tests. memoizedSquare recomputes the expensive function on every call; a repeated argument must reuse the cached result.",
        "notes": "medium · remove-assignment · missing logic · canonical fix in fixtures/breadth-06.FAULT.json"
      },
      "probe": {
        "kind": "check-rerun",
        "check": "fixtures/breadth-06/check.mjs"
      }
    },
    {
      "id": "breadth-07",
      "fixture": "fixtures/breadth-07",
      "task": {
        "prompt": "The workspace at the path below contains a TypeScript package whose vitest suite fails. Fix the source so the vitest suite passes (the scoring harness runs the suite for you; you do not need to install anything). Do not modify the tests. withDefaults leaves retries undefined when the caller omits it; it must default to three.",
        "notes": "medium · default-param-removal · value misuse · canonical fix in fixtures/breadth-07.FAULT.json"
      },
      "probe": {
        "kind": "check-rerun",
        "check": "fixtures/breadth-07/check.mjs"
      }
    },
    {
      "id": "breadth-08",
      "fixture": "fixtures/breadth-08",
      "task": {
        "prompt": "The workspace at the path below contains a TypeScript package whose vitest suite fails. Fix the source so the vitest suite passes (the scoring harness runs the suite for you; you do not need to install anything). Do not modify the tests. cityOf throws when the profile is missing; it must report 'unknown' instead.",
        "notes": "medium · non-null-overreach · function misuse · canonical fix in fixtures/breadth-08.FAULT.json"
      },
      "probe": {
        "kind": "check-rerun",
        "check": "fixtures/breadth-08/check.mjs"
      }
    },
    {
      "id": "breadth-09",
      "fixture": "fixtures/breadth-09",
      "task": {
        "prompt": "The workspace at the path below contains a TypeScript package whose vitest suite fails. Fix the source so the vitest suite passes (the scoring harness runs the suite for you; you do not need to install anything). Do not modify the tests. Callers report that mutating the array returned by snapshot changes what later snapshots contain, and that an order which is only paid or only shipped already reads as complete; snapshots must be independent of their callers and completion must require both paid and shipped.",
        "notes": "hard · shared-reference-return+logical-swap · variable misuse · canonical fix in fixtures/breadth-09.FAULT.json"
      },
      "probe": {
        "kind": "check-rerun",
        "check": "fixtures/breadth-09/check.mjs"
      }
    },
    {
      "id": "breadth-10",
      "fixture": "fixtures/breadth-10",
      "task": {
        "prompt": "The workspace at the path below contains a TypeScript package whose vitest suite fails. Fix the source so the vitest suite passes (the scoring harness runs the suite for you; you do not need to install anything). Do not modify the tests. parseAmount misreads strings such as '0x10', and rangeLabel prints a range's bounds in the wrong order; parsing must be base ten and labels must read from-to.",
        "notes": "hard · radix-coercion-drop+argument-swap · value misuse · canonical fix in fixtures/breadth-10.FAULT.json"
      },
      "probe": {
        "kind": "check-rerun",
        "check": "fixtures/breadth-10/check.mjs"
      }
    },
    {
      "id": "breadth-11",
      "fixture": "fixtures/breadth-11",
      "task": {
        "prompt": "The workspace at the path below contains a TypeScript package whose vitest suite fails. Fix the source so the vitest suite passes (the scoring harness runs the suite for you; you do not need to install anything). Do not modify the tests. truncate returns a shortened string even when the text already fits the limit.",
        "notes": "easy · equality-boundary · operator misuse · canonical fix in fixtures/breadth-11.FAULT.json"
      },
      "probe": {
        "kind": "check-rerun",
        "check": "fixtures/breadth-11/check.mjs"
      }
    },
    {
      "id": "breadth-12",
      "fixture": "fixtures/breadth-12",
      "task": {
        "prompt": "The workspace at the path below contains a TypeScript package whose vitest suite fails. Fix the source so the vitest suite passes (the scoring harness runs the suite for you; you do not need to install anything). Do not modify the tests. toKebabCase returns uppercase letters instead of a lowercase slug.",
        "notes": "easy · method-swap · function misuse · canonical fix in fixtures/breadth-12.FAULT.json"
      },
      "probe": {
        "kind": "check-rerun",
        "check": "fixtures/breadth-12/check.mjs"
      }
    }
  ]
}
