{
  "bug_type": "operator misuse",
  "failure_symptoms": "addCents subtracts instead of adds, and isOverdrawn reports a zero balance as overdrawn.",
  "operator": "arithmetic-swap+equality-boundary",
  "difficulty": "hard",
  "provenance": {
    "origin": "diff-replay",
    "generator": "fixture-git-replay",
    "seed": 120,
    "engine_version": "git-history@f3"
  },
  "validation": {
    "f2p": [
      "adds two cent amounts",
      "treats a zero balance as not overdrawn"
    ],
    "p2p": [
      "formats cents as a two-decimal amount",
      "subtracts the percentage from the amount",
      "applies the rate on top of the amount"
    ],
    "fix": {
      "src/balance.ts": "export function isOverdrawn(balance: number): boolean {\n  return balance < 0;\n}\n",
      "src/money.ts": "export function addCents(a: number, b: number): number {\n  return a + b;\n}\n\nexport function formatCents(cents: number): string {\n  return (cents / 100).toFixed(2);\n}\n"
    }
  },
  "adequacy": {
    "file": "src/money.ts",
    "delete": "return a + b;"
  },
  "tell_audit": {
    "critic": "lane-leader adversarial diff read (tests withheld)",
    "verdict": "told",
    "notes": "Both diffs are one-token; hard tier is carried by finding two independent faults."
  }
}
