{
  "name": "breadth-verified",
  "role": "fixer-worker",
  "provenance": {
    "origin": "catalog-built synthetic (F2 seed + F3, 2026-09-21): 6 easy / 4 medium / 2 hard; every fault independently reproduced from FAULT.json alone (see REPRODUCTION.md); zero-dependency multi-module TS packages; no public-benchmark content"
  },
  "cases": [
    {
      "id": "breadth-01",
      "fixture": "fixtures/breadth-01",
      "task": {
        "prompt": "The workspace at the path below contains a TypeScript package whose vitest suite fails. Fix the source so the vitest suite passes (the scoring harness runs the suite for you; you do not need to install anything). Do not modify the tests. rectangleArea(3, 4, 'm') returns 7.",
        "notes": "easy · arithmetic-swap · operator misuse · canonical fix in fixtures/breadth-01.FAULT.json"
      },
      "probe": {
        "kind": "check-rerun",
        "check": "fixtures/breadth-01/check.mjs"
      }
    },
    {
      "id": "breadth-02",
      "fixture": "fixtures/breadth-02",
      "task": {
        "prompt": "The workspace at the path below contains a TypeScript package whose vitest suite fails. Fix the source so the vitest suite passes (the scoring harness runs the suite for you; you do not need to install anything). Do not modify the tests. For a three-page document, isLastPage says page index 2 is not the last page.",
        "notes": "easy · equality-boundary · operator misuse · canonical fix in fixtures/breadth-02.FAULT.json"
      },
      "probe": {
        "kind": "check-rerun",
        "check": "fixtures/breadth-02/check.mjs"
      }
    },
    {
      "id": "breadth-03",
      "fixture": "fixtures/breadth-03",
      "task": {
        "prompt": "The workspace at the path below contains a TypeScript package whose vitest suite fails. Fix the source so the vitest suite passes (the scoring harness runs the suite for you; you do not need to install anything). Do not modify the tests. Members are charged the full price while non-members receive a discount.",
        "notes": "easy · ternary-swap · operator misuse · canonical fix in fixtures/breadth-03.FAULT.json"
      },
      "probe": {
        "kind": "check-rerun",
        "check": "fixtures/breadth-03/check.mjs"
      }
    },
    {
      "id": "breadth-04",
      "fixture": "fixtures/breadth-04",
      "task": {
        "prompt": "The workspace at the path below contains a TypeScript package whose vitest suite fails. Fix the source so the vitest suite passes (the scoring harness runs the suite for you; you do not need to install anything). Do not modify the tests. backoffDelay(100, 3) returns 2700.",
        "notes": "easy · constant-delta (trivial-prone, easy-only) · value misuse · canonical fix in fixtures/breadth-04.FAULT.json"
      },
      "probe": {
        "kind": "check-rerun",
        "check": "fixtures/breadth-04/check.mjs"
      }
    },
    {
      "id": "breadth-05",
      "fixture": "fixtures/breadth-05",
      "task": {
        "prompt": "The workspace at the path below contains a TypeScript package whose vitest suite fails. Fix the source so the vitest suite passes (the scoring harness runs the suite for you; you do not need to install anything). Do not modify the tests. allInStock reports true for a catalog that still has an out-of-stock item.",
        "notes": "medium · method-swap · function misuse · canonical fix in fixtures/breadth-05.FAULT.json"
      },
      "probe": {
        "kind": "check-rerun",
        "check": "fixtures/breadth-05/check.mjs"
      }
    },
    {
      "id": "breadth-06",
      "fixture": "fixtures/breadth-06",
      "task": {
        "prompt": "The workspace at the path below contains a TypeScript package whose vitest suite fails. Fix the source so the vitest suite passes (the scoring harness runs the suite for you; you do not need to install anything). Do not modify the tests. Two calls to memoizedSquare with the same argument run the expensive function twice.",
        "notes": "medium · remove-assignment · missing logic · canonical fix in fixtures/breadth-06.FAULT.json"
      },
      "probe": {
        "kind": "check-rerun",
        "check": "fixtures/breadth-06/check.mjs"
      }
    },
    {
      "id": "breadth-07",
      "fixture": "fixtures/breadth-07",
      "task": {
        "prompt": "The workspace at the path below contains a TypeScript package whose vitest suite fails. Fix the source so the vitest suite passes (the scoring harness runs the suite for you; you do not need to install anything). Do not modify the tests. withDefaults leaves retries undefined when the caller omits it.",
        "notes": "medium · default-param-removal · value misuse · canonical fix in fixtures/breadth-07.FAULT.json"
      },
      "probe": {
        "kind": "check-rerun",
        "check": "fixtures/breadth-07/check.mjs"
      }
    },
    {
      "id": "breadth-08",
      "fixture": "fixtures/breadth-08",
      "task": {
        "prompt": "The workspace at the path below contains a TypeScript package whose vitest suite fails. Fix the source so the vitest suite passes (the scoring harness runs the suite for you; you do not need to install anything). Do not modify the tests. cityOf throws a TypeError when the profile is missing.",
        "notes": "medium · non-null-overreach · function misuse · canonical fix in fixtures/breadth-08.FAULT.json"
      },
      "probe": {
        "kind": "check-rerun",
        "check": "fixtures/breadth-08/check.mjs"
      }
    },
    {
      "id": "breadth-09",
      "fixture": "fixtures/breadth-09",
      "task": {
        "prompt": "The workspace at the path below contains a TypeScript package whose vitest suite fails. Fix the source so the vitest suite passes (the scoring harness runs the suite for you; you do not need to install anything). Do not modify the tests. Pushing onto the array returned by snapshot changes what later snapshots contain, and an order that is paid but not yet shipped already reads as complete.",
        "notes": "hard · shared-reference-return+logical-swap · variable misuse · canonical fix in fixtures/breadth-09.FAULT.json"
      },
      "probe": {
        "kind": "check-rerun",
        "check": "fixtures/breadth-09/check.mjs"
      }
    },
    {
      "id": "breadth-10",
      "fixture": "fixtures/breadth-10",
      "task": {
        "prompt": "The workspace at the path below contains a TypeScript package whose vitest suite fails. Fix the source so the vitest suite passes (the scoring harness runs the suite for you; you do not need to install anything). Do not modify the tests. parseAmount('0x10') returns 16, and rangeLabel(3, 7) prints 'range 7-3'.",
        "notes": "hard · radix-coercion-drop+argument-swap · value misuse · canonical fix in fixtures/breadth-10.FAULT.json"
      },
      "probe": {
        "kind": "check-rerun",
        "check": "fixtures/breadth-10/check.mjs"
      }
    },
    {
      "id": "breadth-11",
      "fixture": "fixtures/breadth-11",
      "task": {
        "prompt": "The workspace at the path below contains a TypeScript package whose vitest suite fails. Fix the source so the vitest suite passes (the scoring harness runs the suite for you; you do not need to install anything). Do not modify the tests. truncate returns a shortened string even when the text already fits the limit.",
        "notes": "easy · equality-boundary · operator misuse · canonical fix in fixtures/breadth-11.FAULT.json"
      },
      "probe": {
        "kind": "check-rerun",
        "check": "fixtures/breadth-11/check.mjs"
      }
    },
    {
      "id": "breadth-12",
      "fixture": "fixtures/breadth-12",
      "task": {
        "prompt": "The workspace at the path below contains a TypeScript package whose vitest suite fails. Fix the source so the vitest suite passes (the scoring harness runs the suite for you; you do not need to install anything). Do not modify the tests. toKebabCase returns uppercase letters instead of a lowercase slug.",
        "notes": "easy · method-swap · function misuse · canonical fix in fixtures/breadth-12.FAULT.json"
      },
      "probe": {
        "kind": "check-rerun",
        "check": "fixtures/breadth-12/check.mjs"
      }
    }
  ]
}
